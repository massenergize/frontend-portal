import { oneEventPageComponentsRenderProperly } from "../../../support/M.E/utils";
import "../../reusable/go-to-one-event-page(via navigation).spec";

describe("One event  page content displays properly when visited via nav menu", () => {
  oneEventPageComponentsRenderProperly();
});
