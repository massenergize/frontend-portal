import { showThatAllEventCardsDisplayProperly } from "../../support/M.E/utils";
import "./reusable/go-to-all-events-page(via navigation).spec";
describe("Cards render properly on all events page when visited via nav menu", function() {
  showThatAllEventCardsDisplayProperly();
});
