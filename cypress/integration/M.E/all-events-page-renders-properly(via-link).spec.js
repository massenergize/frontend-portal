import { showThatAllEventCardsDisplayProperly } from "../../support/M.E/utils";
import "./reusable/go-to-all-events-page(via link).spec";

describe("All event cards render properly, or notification if no cards", function () {
  showThatAllEventCardsDisplayProperly();
});
